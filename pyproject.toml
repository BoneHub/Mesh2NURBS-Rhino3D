[build-system]
requires = ["setuptools>=64"]
build-backend = "setuptools.build_meta"

[project]
name = "mesh2nurbs_rhino3d"
version = "1.0"
dependencies = []

[tool.setuptools]
packages = ["mesh2nurbs_rhino3d"]

[project.scripts]
mesh2nurbs-rhino3d = "mesh2nurbs_rhino3d.mesh2nurbs_rhino3d:cli_entry_point"